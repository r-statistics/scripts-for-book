#Тест при равных дисперсиях дисперсии

#генерация выбюорок с помошью генератора случаных чисел

x1<-rnorm(30, 15, 2)
x2<-rnorm(24, 16, 2)

#тест на нормальность распределения

shapiro.test(x1)
shapiro.test(x2)

#тест на различие дисперсий

var.test(x1,x2)

#t тест Стьюдента

t.test(x1,x2, var.equal=T)
